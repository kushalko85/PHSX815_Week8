import numpy as np
import matplotlib.pyplot as plt

N_meas = 10
N_exp = 100
true_value = []
meas_value = []

#loop over parameters
for i in range (1,100):
    true_value1 = float(i)/10.
    for j in range(0,N_exp):
        meas_value1 = 0.
        for k in range(0,N_meas):

            x = np.random.exponential(true_value,1)
            meas_value = Meas_value + x 

        meas_value = float(meas_value)/float(N_meas)
        meas_value.append(float(meas_value))
        true_value.append(float(true_value))


#Plotting stuffs
plt.figure()
plt.hist2d(true_value,meas_value)
ax.set_facecolor('white')
plt.xlabel('True Value of ' + r'$\lambda$')
ax = plt.axes()
plt.ylabel('Measured Value of' + r'$\lambda$')
plt.title('Neyman Construction (Poission Distribution) ')
plt.show()
